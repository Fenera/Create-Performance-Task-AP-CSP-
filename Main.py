import pygame

pygame.init()
pygame.font.init()

length = 600
width = 800

#Creates window
window = pygame.display.set_mode((length, width))

#colors
red = (255, 0, 0)
green = (0, 255, 0)
blue = (0, 0, 255)

run = True

while run:
    

